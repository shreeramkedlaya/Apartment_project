/**
 * Core HTTP configuration constants
 */

export const BASE_URL = 'http://localhost:8000';

// ─── Frontend Version Management ─────────────────────────────────────────────
export const MAIN_VERSION = 1;
export const SUB_VERSION = 0;
export const FRONTEND_VERSION = `${MAIN_VERSION}.${SUB_VERSION}`;
